<img src="https://static.vecteezy.com/system/resources/previews/000/551/670/non_2x/double-cheese-burger-vector.jpg">

<h2>Burger Eater!</h2>

<div class = "container">
    <div class = "row">
        <div class = "col-sm-6">
            <ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgers-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>
        </div>
        <div class="col-sm-6">
            <ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burgers-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>
        </div>
    </div>
</div>
<div id="burger-form">
<h1>Add a Burger!</h1>
<form class = "create-form">
  <div class="form-group">
    <textarea type="text" id="burger-name" name="name"></textarea>
  </div>
  <button type="submit">Add Burger</button>
</form>
</div>